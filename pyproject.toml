[project]
name = "reports-app"
version = "0.1.0"
description = "Web app de relatórios com autenticação JWT e MySQL"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "python-jose[cryptography]>=3.3.0",
    "bcrypt>=4.0.0",
    "python-multipart>=0.0.9",
    "aiomysql>=0.2.0",
    "python-dotenv>=1.0.0",
    "jinja2>=3.1.4",
]

[tool.uv]
dev-dependencies = []

# bcrypt direto, sem passlib (compativel com Python 3.14)
